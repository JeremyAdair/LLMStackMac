services:
  redisinsight:
    image: redis/redisinsight:latest
    ports:
      - "5540:5540"
    volumes:
      # Persist RedisInsight connections/workspace state on host.
      - ${LLMSTACK_HOST_ROOT:-C:/llm-stack}/redisinsight/data:/data
    networks:
      - llm-stack
