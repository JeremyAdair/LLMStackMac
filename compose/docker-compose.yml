name: llm-stack

networks:
  llm-stack:
    driver: bridge

volumes:
  ollama_data:
  openwebui_data:
  qdrant_data:
  postgres_data:
  redis_data:
  flowise_data:
  openhands_data:
  prometheus_data:
  grafana_data:
