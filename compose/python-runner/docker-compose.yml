services:
  python-runner:
    build:
      context: ..
      dockerfile: compose/python-runner/Dockerfile
    volumes:
      - ../workspace:/workspace
      - ../scripts:/scripts
    networks:
      - llm-stack
    profiles:
      - tools

