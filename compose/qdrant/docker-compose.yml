services:
  qdrant:
    image: qdrant/qdrant@sha256:f1c7272cdac52b38c1a0e89313922d940ba50afd90d593a1605dbbc214e66ffb
    # Internal-only access; use the service name from other containers.
    expose:
      - "6333"
    volumes:
      # Persist vector index and payload data.
      - ${QDRANT_DATA_MOUNT:-qdrant_data}:/qdrant/storage
    networks:
      - llm-stack
