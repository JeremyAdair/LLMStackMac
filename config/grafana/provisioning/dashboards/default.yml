apiVersion: 1

providers:
  - name: LLMStack Dashboards
    orgId: 1
    folder: LLMStack
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
